# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts



# claude code — machine-specific / generated (keep settings.json, CLAUDE.md)
.claude/*
# 슬래시가 들어간 패턴(.omc/*)은 루트에만 걸린다.
# 하위 디렉터리에도 생기므로(docs/.omc 등) 디렉터리명만으로 매칭시킨다.
.omc/
.arc/
# skills CLI 정본 저장소 — .claude/skills로 복사되므로 중복이다.
# 재현은 skills-lock.json으로 한다 (npx skills experimental_install).
.agents/
.gstack/
# 브레인스토밍 비주얼 컴패니언 세션 산출물 — 로컬 목업 전용
.superpowers/
