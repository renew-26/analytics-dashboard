/**
 * 차트 공용 규칙.
 *
 * Recharts 는 시리즈마다 기본 1500ms 애니메이션이 켜져 있고, 데이터가 바뀔 때마다
 * 처음부터 다시 재생한다. 이 대시보드는 BM 필터·연도 토글·카테고리 탭이 화면마다
 * 깔려 있어 한 세션에 수십 번 밟히는데, 그때마다 1.5초가 재생되면 필터 자체가
 * 느린 것으로 인지된다. 자주 보는 애니메이션은 없애는 쪽이 맞다.
 *
 * 시리즈 15개가 9개 파일에 흩어져 있어 값을 여기 하나만 둔다.
 * 사용: <Line {...CHART_ANIM} ... />
 */
export const CHART_ANIM = { isAnimationActive: false } as const;
